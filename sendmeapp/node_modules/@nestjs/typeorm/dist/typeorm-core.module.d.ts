import { DynamicModule, OnModuleDestroy } from '@nestjs/common';
import { ModuleRef } from '@nestjs/core';
import { TypeOrmModuleAsyncOptions, TypeOrmModuleOptions } from './interfaces/typeorm-options.interface';
export declare class TypeOrmCoreModule implements OnModuleDestroy {
    private readonly options;
    private readonly moduleRef;
    constructor(options: TypeOrmModuleOptions, moduleRef: ModuleRef);
    static forRoot(options?: TypeOrmModuleOptions): DynamicModule;
    static forRootAsync(options: TypeOrmModuleAsyncOptions): DynamicModule;
    onModuleDestroy(): Promise<void>;
    private static createAsyncProviders(options);
    private static createAsyncOptionsProvider(options);
    private static createEntityManagerProvider(options);
    private static createConnectionFactory(options);
}
